# VoltHacks day 4 report — 2026-09-08 (repo @ 0bdf2b0 → fa05116)

Goal: video recording + Devpost copy. Both shipped. NOT submitted (parent review first).

## Shipped

1. **Demo video** — `artifacts/video/sensormesh-demo.mp4` (99.7s, 1600×900, 2.9 MB). One continuous take, 8 beats per `docs/video-script.md`: problem slide → simulator terminal (typed command, real output) → MCP query terminal (real `queryReadings` output with sha256) → dashboard browse → live Buy flow (402 → sign → 200 + settlement, real round trip against :8793, "mock settlement (dev)" pill visible) → architecture (README diagram verbatim) → three tiers → repo card. Every terminal frame = real captured output from the repo, regenerated by the harness. Silent (no TTS on box); every beat carries the script's voiceover line as an on-screen caption bar.
2. **Slide deck** — `artifacts/deck/01..08.png` (same 8 beats; slides 4/5 shot from the live dashboard post-flow). PNG ready for Devpost presentation upload.
3. **Devpost copy draft** — `state/volt-devpost-draft.md`: name, tagline, ~300-word description (problem → solution → tiers → architecture → honest mock-settlement scope), judging-criteria mapping, other form fields. Slop check run twice (both PASS, notes in the file).
4. **Harness** — `artifacts/video/record.py` + `deck.py` committed.

## Recording path (for the record)

Headed chromium on the proven Wayland pattern (running headless-weston session, `WAYLAND_DISPLAY=wayland-1`), Playwright `record_video_dir` screencast → webm → ffmpeg → H.264 mp4. NOT headless: real browser window on a real compositor, frames verified painted. Camoufox tried first — its screencast returns black frames on weston/pixman; dashboard is localhost so no CF, chromium headed is equivalent. Xvfb path untouched (xkbcomp hang risk from prior post-mortem).

## Verified

- Buy flow re-verified live in headed chromium: step3 "data delivered, settlement proof in X-PAYMENT-RESPONSE", tx `0xmock1a07f1e93df`, rows rendered.
- Video frames probed at 10 timestamps (painted, right color signatures per beat).
- Health: 1,296 rows, sha `022b54f9…a1197`, mock_settlement=true — data files untouched (simulator ran with `--out /tmp/smv/v42`).

## Not done

- Devpost submit — copy is a draft for parent review only.
- Real facilitator + funded Base wallet — still the only unshown piece; video/copy don't claim it.
- Voiceover audio — no TTS available; captions carry the script. Parent call: upload as-is or add narration later.

## State

- Pushed to github.com/jayjex/sensormesh (master) @ fa05116.
- Services untouched (api :8793 + tunnel up; static server :8018 started for the recording, kill if not needed: `pkill -f "http.server 8018"`).
